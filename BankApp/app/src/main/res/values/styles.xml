<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <!--<item name="colorAccent">@color/colorAccent</item>-->
        <item name="colorAccent">@color/colorPrimary</item>

        <!--<item name="textColorPrimary">@color/textColorPrimary</item>-->
        <!--<item name="textColorSecondary">@color/textColorSecondary</item>-->

        <item name="android:windowBackground">@color/activityBackground</item>
        
        <!-- TextInputLayout -->
        <item name="android:titleTextStyle">@style/WhiteTextInputLayout</item>
    </style>


    <style name="WhiteTextInputLayout" parent="Widget.Design.TextInputLayout">
        <item name="android:background">@color/activityBackground</item>
    </style>

    <style name="ThemeOverlay.FirebaseIcon" parent="AppTheme">
        <item name="android:layout_marginTop">@dimen/icon_top_margin</item>
        <item name="android:layout_marginBottom">@dimen/icon_bottom_margin</item>
    </style>
    <style name="ThemeOverlay.MyTitleText" parent="AppTheme">
        <item name="android:gravity">center</item>
        <item name="android:textSize">36sp</item>
    </style>
</resources>
